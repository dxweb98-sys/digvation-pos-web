import type { ReactNode } from 'react';
import { cn } from '../cn';
export interface StatusOption { label: ReactNode; value: string; count?: number; }
export interface StatusFilterProps { label?: ReactNode; options: readonly StatusOption[]; value: string; onChange: (value: string) => void; allLabel?: ReactNode; }
export function StatusFilter({ label, options, value, onChange, allLabel = 'Semua' }: StatusFilterProps) { const button = (active: boolean) => cn('rounded-lg px-3 py-1.5 text-xs font-medium transition-all duration-150', active ? 'bg-[var(--color-brand)] text-white shadow-sm' : 'border border-[var(--color-border)] text-[var(--color-text-muted)] hover:bg-[var(--color-surface-muted)] hover:text-[var(--color-text)]'); return <div className="flex flex-wrap items-center gap-3">{label ? <span className="text-xs font-medium text-[var(--color-text-muted)]">{label}</span> : null}<div className="flex flex-wrap items-center gap-1.5"><button type="button" onClick={() => onChange('')} className={button(!value)}>{allLabel}</button>{options.map((option) => <button type="button" key={option.value} onClick={() => onChange(option.value)} className={button(value === option.value)}>{option.label}{option.count !== undefined ? <span className="ml-1 opacity-70">({option.count})</span> : null}</button>)}</div></div>; }
